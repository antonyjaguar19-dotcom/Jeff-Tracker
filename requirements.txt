# Inference. Torch is left to you -- install the build that matches your CUDA.
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
einops>=0.7
numpy>=1.24
opencv-python>=4.8
huggingface_hub>=0.20

# Training only (Kubric/MOVi-E is distributed as TFRecord shards, so reading it
# needs tensorflow even though nothing is trained with it). Install these into a
# separate prefix -- see METHOD.md.
#   tensorflow-cpu
#   tensorflow_datasets
